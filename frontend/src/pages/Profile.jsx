import { useEffect, useState } from "react";
import DashboardLayout from "../components/DashboardLayout";
import Toast from "../components/Toast";

function Profile() {
  const [profile, setProfile] = useState(null);
  const [name, setName] = useState("");
  const [saving, setSaving] = useState(false);
  const [message, setMessage] = useState("");
  const [isError, setIsError] = useState(false);
  const headers = () => ({ Authorization: "Bearer " + sessionStorage.getItem("token") });
  const notify = (text, error = false) => { setIsError(error); setMessage(text); };
  const load = async () => {
    try { const response = await fetch("http://localhost:8080/api/users/profile", { headers: headers() }); const data = await response.json(); if (!response.ok) throw new Error(data.message); setProfile(data); setName(data.name || ""); }
    catch (error) { notify(error.message || "Unable to load profile.", true); }
  };
  useEffect(() => { load(); }, []);
  useEffect(() => { if (!message) return undefined; const timer = setTimeout(() => setMessage(""), 3500); return () => clearTimeout(timer); }, [message]);
  const update = async event => {
    event.preventDefault();
    if (!name.trim()) return notify("Name is required.", true);
    setSaving(true);
    try { const response = await fetch("http://localhost:8080/api/users/profile", { method: "PUT", headers: { ...headers(), "Content-Type": "application/json" }, body: JSON.stringify({ name: name.trim() }) }); const data = await response.json(); if (!response.ok) throw new Error(data.message); setProfile(data); sessionStorage.setItem("userName", data.name); notify("Profile updated successfully."); }
    catch (error) { notify(error.message || "Unable to update profile.", true); } finally { setSaving(false); }
  };
  const stats = profile ? [["Decisions created", profile.decisionsCreated], ["Votes cast", profile.votesParticipated], ["Communities joined", profile.joinedCommunities]] : [];
  return <DashboardLayout pageTitle="Profile" pageSubtitle="Your DecisionHub identity, activity, and account settings.">
    <Toast message={message} isError={isError} />
    <style>{`
      .profile-page { max-width: 1250px; margin: 0 auto; padding: 4px 0 42px; color: #f8fafc; }.profile-loading { padding: 54px; border: 1px dashed #3a314a; border-radius: 16px; color: #958d9f; text-align: center; }.profile-banner { position: relative; overflow: hidden; display: flex; align-items: center; gap: 20px; padding: 27px 30px; border: 1px solid #31294a; border-radius: 18px; background: radial-gradient(circle at 90% 20%,rgba(79,70,229,.3),transparent 25%),linear-gradient(135deg,#1b1630,#100d19); }.profile-banner::after { content:""; position:absolute; right:-50px; bottom:-62px; width:190px; height:190px; border:1px solid rgba(167,139,250,.15); border-radius:50%; }.profile-avatar { position: relative; z-index: 1; display:flex; align-items:center; justify-content:center; width:76px; height:76px; flex:0 0 76px; border:2px solid rgba(196,181,253,.4); border-radius:24px; background:linear-gradient(135deg,#4f46e5,#7c3aed); box-shadow:0 12px 28px rgba(79,70,229,.28); font-size:28px; font-weight:800; }.profile-banner-copy { position:relative; z-index:1; min-width:0; }.profile-kicker { color:#c5b4ff; font-size:10px; font-weight:700; letter-spacing:1.4px; text-transform:uppercase; }.profile-banner h2 { margin:7px 0 4px; font-size:25px; letter-spacing:-.5px; }.profile-banner p { margin:0; color:#9d95ad; font-size:13px; }.profile-role { position:relative; z-index:1; margin-left:auto; border:1px solid rgba(110,231,183,.24); border-radius:20px; padding:7px 11px; color:#9af0c3; background:rgba(16,102,71,.16); font-size:10px; font-weight:700; }.profile-grid { display:grid; grid-template-columns: .85fr 1.15fr; gap:18px; margin-top:19px; }.profile-card { border:1px solid #2d263e; border-radius:16px; background:linear-gradient(145deg,#181421,#12101a); padding:23px; }.profile-card h3 { margin:0; color:#f2edf8; font-size:16px; }.profile-card > p { margin:6px 0 20px; color:#8f879d; font-size:11px; }.account-row { padding:13px 0; border-bottom:1px solid #2a2437; }.account-row:last-child { border-bottom:0; }.account-label { display:block; color:#82798d; font-size:10px; font-weight:700; letter-spacing:.3px; text-transform:uppercase; }.account-value { display:block; margin-top:5px; color:#e3ddea; font-size:13px; overflow-wrap:anywhere; }.profile-form { display:grid; gap:16px; }.profile-form label { color:#aaa1b6; font-size:11px; font-weight:600; }.profile-form input { box-sizing:border-box; display:block; width:100%; margin-top:8px; border:1px solid #383047; border-radius:9px; outline:0; background:#0e0c14; color:#f1ecf9; padding:11px 12px; font-size:13px; }.profile-form input:focus { border-color:#7753bd; box-shadow:0 0 0 3px rgba(124,58,237,.12); }.profile-form input:disabled { color:#888091; cursor:not-allowed; }.save-profile { justify-self:start; border:0; border-radius:9px; padding:11px 15px; color:white; background:linear-gradient(135deg,#4f46e5,#7c3aed); font-size:12px; font-weight:700; cursor:pointer; }.save-profile:disabled { opacity:.6; cursor:default; }.profile-stats { display:grid; grid-template-columns:repeat(3,1fr); gap:13px; margin-top:18px; }.profile-stat { position:relative; overflow:hidden; border:1px solid #2d263e; border-radius:14px; background:#15121f; padding:18px; }.profile-stat::after { content:""; position:absolute; top:-20px; right:-18px; width:65px; height:65px; border-radius:50%; background:rgba(124,58,237,.12); filter:blur(6px); }.profile-stat strong { position:relative; display:block; color:#ded1ff; font-size:25px; }.profile-stat span { position:relative; display:block; margin-top:5px; color:#8b8394; font-size:11px; } @media(max-width:750px){.profile-banner{padding:22px 19px}.profile-grid{grid-template-columns:1fr}.profile-stats{grid-template-columns:1fr}.profile-role{display:none}} @media(max-width:470px){.profile-avatar{width:60px;height:60px;flex-basis:60px;border-radius:19px;font-size:23px}.profile-banner h2{font-size:20px}}
    `}</style>
    <div className="profile-page">{!profile ? <div className="profile-loading">Loading your profile…</div> : <><section className="profile-banner"><div className="profile-avatar">{profile.name?.charAt(0)?.toUpperCase() || "U"}</div><div className="profile-banner-copy"><span className="profile-kicker">DecisionHub member</span><h2>{profile.name}</h2><p>{profile.email}</p></div><span className="profile-role">{profile.role}</span></section><div className="profile-grid"><section className="profile-card"><h3>Account at a glance</h3><p>Your DecisionHub membership details.</p><div className="account-row"><span className="account-label">Email address</span><span className="account-value">{profile.email}</span></div><div className="account-row"><span className="account-label">Account role</span><span className="account-value">{profile.role}</span></div><div className="account-row"><span className="account-label">Member since</span><span className="account-value">{profile.createdAt ? new Date(profile.createdAt).toLocaleDateString() : "Not available"}</span></div></section><section className="profile-card"><h3>Personal details</h3><p>Keep the name shown across your decisions and comments up to date.</p><form className="profile-form" onSubmit={update}><label>Display name<input value={name} onChange={event => setName(event.target.value)} maxLength="100" /></label><label>Email address<input value={profile.email} disabled /></label><button className="save-profile" disabled={saving}>{saving ? "Saving changes…" : "Save changes"}</button></form></section></div><section className="profile-stats">{stats.map(([label, value]) => <div className="profile-stat" key={label}><strong>{value || 0}</strong><span>{label}</span></div>)}</section></>}</div>
  </DashboardLayout>;
}
export default Profile;
